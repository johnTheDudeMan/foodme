<% provide(:title, "FOODsters") %>
<h1>FOODsters <small>who signed up</small></h1>

<div class="row">
	<div class="col-md-10">
		<%= will_paginate %>
		<ul>
			<%= render @users %>
		</ul>
		<%= will_paginate %>
	</div>
</div>
